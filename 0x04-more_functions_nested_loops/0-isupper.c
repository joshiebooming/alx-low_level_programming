#include "main.h"
/**
*_isupper - check if c is upper case
*@c: parameter getting checked
* Return: 0 or 1 on condition
*/
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z')
		return (1);
}
	return (0);
}
