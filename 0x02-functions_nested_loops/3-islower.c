#include "main.h"
/**
  *_islower - checks if c is lowercase
  *@c: - interger to be checked
  * Return: - "1" or "0" depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
