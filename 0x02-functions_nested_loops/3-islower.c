#include "main.h"
/**
  *_islower-->checks if c is lowercase
  *Return - Returns  1 or 0 depending on condition
  * c - interger
  * Return "1" or "0"
 **/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
